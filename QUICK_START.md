# ⚡ Быстрый старт - Сборка проекта

## 📌 Release режим (для production - без debug вывода)

```bash
# Полная пересборка
rm -rf build
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build

# Запуск
./build/Correlator
```

**✅ В Release режиме отключено:**
- `[PROFILE]` - профилирование операций
- `[DEBUG]` - все debug сообщения
- `PROFILING RESULTS` - результаты профилирования

**✅ Остаётся только:**
- `[STEP 1]`, `[OK]`, `[FFT]` - важные сообщения
- Вывод из `main.cpp` - пользовательский интерфейс

---

## 🐛 Debug режим (для отладки - с полным выводом)

```bash
# Полная пересборка
rm -rf build_debug
cmake -B build_debug -DCMAKE_BUILD_TYPE=Debug
cmake --build build_debug

# Запуск
./build_debug/Correlator
```

**✅ В Debug режиме включено:**
- ВСЁ! Все debug сообщения, профилирование, диагностика

---

## 🎯 Через VS Code

1. **Откройте проект в VS Code**
2. **Нажмите `F5`**
3. **Выберите:**
   - `Launch Correlator (Release)` - для Release
   - `Launch Correlator (Debug)` - для Debug

---

## ✅ Проверка что Release работает правильно

```bash
# Собрать Release
rm -rf build && cmake -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build

# Проверить что DEBUG вывод отключен
./build/Correlator 2>&1 | grep -E "\[PROFILE\]|\[DEBUG\]|PROFILING RESULTS" && echo "❌ Проблема!" || echo "✅ Всё правильно - DEBUG отключен!"
```

---

**📖 Полная инструкция:** см. `BUILD_INSTRUCTIONS.md`

