# 🚀 OPENCL CAPABILITY CHECKER - ИНСТРУКЦИЯ

## 📋 СТРУКТУРА ФАЙЛОВ

```
correlator-test/
├── opencl_capability_checker.cpp   (тестовый код)
├── CMakeLists.txt                  (конфиг сборки)
└── README.md                        (этот файл)
```

---

## 🔧 СБОРКА

### На Linux (Ubuntu/Debian):

```bash
# 1. Установить зависимости
sudo apt-get install -y \
    cmake \
    build-essential \
    opencl-headers \
    ocl-icd-opencl-dev

# 2. Создать папку сборки
mkdir build
cd build

# 3. Сконфигурировать CMake
cmake ..

# 4. Собрать
make

# 5. Запустить тест
./capability_checker
```

### На Windows (Visual Studio):

```bash
# 1. Убедиться что установлены:
#    - Visual Studio 2019+ или GCC
#    - CMake (https://cmake.org/download/)
#    - OpenCL SDK от NVIDIA (https://developer.nvidia.com/opencl)

# 2. Создать папку сборки
mkdir build
cd build

# 3. Сконфигурировать (для Visual Studio 2022)
cmake .. -G "Visual Studio 17 2022"

# 4. Собрать
cmake --build . --config Release

# 5. Запустить
Release\capability_checker.exe
```

### На macOS:

```bash
# 1. Установить Homebrew если нет
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2. Установить зависимости
brew install cmake

# 3. Обычно OpenCL уже встроен в macOS
# Если нет: brew install opencl-icd-loader

# 4. Сборка
mkdir build
cd build
cmake ..
make

# 5. Запустить
./capability_checker
```

---

## 🎯 ПРИМЕРЫ ВЫВОДА

### ПРИМЕР 1: RTX 2080 Ti (NVIDIA) ✅ OpenCL 1.2

```
╔════════════════════════════════════════════════════════╗
║  OpenCL DEVICE CAPABILITY CHECKER FOR CORRELATOR       ║
║  Проверка поддержки Command Buffers и SVM              ║
╚════════════════════════════════════════════════════════╝

Found 1 platform(s)

╔════════════════════════════════════════════════════════╗
║ PLATFORM 0: NVIDIA CUDA (NVIDIA Corporation)
╚════════════════════════════════════════════════════════╝

────────────────────────────────────────────────────────
DEVICE 0
────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════
📊 DEVICE INFO
═══════════════════════════════════════════════════════
Name:              GeForce RTX 2080 Ti
Vendor:            NVIDIA Corporation
Type:              GPU
Compute Units:     68
Global Memory:     11019 MB
Max Work Group:    1024

🔍 OpenCL VERSION
─────────────────────────────────────────────────────
Device Version: OpenCL 1.2 CUDA
Parsed Version:  1.2
⚠️  OpenCL 1.2 (SVM not available, Command Buffers - NO)

📦 EXTENSIONS
─────────────────────────────────────────────────────
❌ cl_khr_command_buffer
✅ cl_khr_svm        ← только если поддерживается
❌ cl_ext_device_fission
✅ cl_nv_device_attribute_query

💾 SHARED VIRTUAL MEMORY (SVM) SUPPORT
─────────────────────────────────────────────────────
❌ No SVM support

🎯 COMMAND BUFFERS (OpenCL 3.0) SUPPORT
─────────────────────────────────────────────────────
❌ cl_khr_command_buffer NOT FOUND
❌ COMMAND BUFFERS ARE NOT SUPPORTED

ℹ️  Try alternative: Asynchronous queues without clWaitForEvents()

🔗 UNIFIED MEMORY SUPPORT
─────────────────────────────────────────────────────
⚠️  Host Unified Memory is NOT supported

🎯 RECOMMENDATIONS FOR YOUR ALGORITHM
═══════════════════════════════════════════════════════
✅ OPTION 2: Use Asynchronous Queues + SVM
   - Skip clWaitForEvents() between steps
   - Use SVM for fine-grain memory management
   - Expected speedup: 5-15%

✅ OPTION 3 (FALLBACK): Asynchronous Enqueue
   - Use clEnqueueWriteBuffer() without waiting
   - Call clFinish() only at the end
   - Expected speedup: 2-5%

╔════════════════════════════════════════════════════════╗
║  SUMMARY AND NEXT STEPS                                ║
╚════════════════════════════════════════════════════════╝

📝 NEXT STEPS:
1. Check which devices support Command Buffers (cl_khr_command_buffer)
2. If supported: Implement 2-buffer approach (STEP1, STEP2+3)
3. If not supported: Use asynchronous enqueue without clWaitForEvents()
4. Expected speedup with Command Buffers: 10-25%

💡 FOR YOUR RTX 2080 Ti / RTX 3060:
- RTX 2080 Ti likely supports OpenCL 1.2 (may not have CB)
- RTX 3060 supports OpenCL 1.2 (may not have CB)
- Fallback: Use async approach for 2-5% speedup

✅ Test completed!
```

---

### ПРИМЕР 2: RTX 3090 (NVIDIA) ❌ OpenCL 1.2, НО с хорошей SVM

```
🔍 OpenCL VERSION
─────────────────────────────────────────────────────
Device Version: OpenCL 1.2 CUDA
Parsed Version:  1.2
⚠️  OpenCL 1.2 (SVM not available, Command Buffers - NO)

💾 SHARED VIRTUAL MEMORY (SVM) SUPPORT
─────────────────────────────────────────────────────
⚠️  CL_DEVICE_SVM_COARSE_GRAIN_BUFFER (limited)
✅ CL_DEVICE_SVM_FINE_GRAIN_BUFFER (good!)

🎯 COMMAND BUFFERS (OpenCL 3.0) SUPPORT
─────────────────────────────────────────────────────
❌ cl_khr_command_buffer NOT FOUND
❌ COMMAND BUFFERS ARE NOT SUPPORTED

🎯 RECOMMENDATIONS FOR YOUR ALGORITHM
═══════════════════════════════════════════════════════
✅ OPTION 2 (BEST): Use Asynchronous Queues + Fine-Grain SVM
   - Skip clWaitForEvents() between steps
   - Use fine-grain SVM (excellent support!)
   - Expected speedup: 10-15% (лучше чем OPTION 3!)

✅ OPTION 3: Asynchronous Enqueue
   - Use clEnqueueWriteBuffer() without waiting
   - Call clFinish() only at the end
   - Expected speedup: 2-5%
```

---

### ПРИМЕР 3: AMD Radeon RX 6800 XT ✅ OpenCL 3.0 с Command Buffers

```
🔍 OpenCL VERSION
─────────────────────────────────────────────────────
Device Version: OpenCL 3.0
Parsed Version:  3.0
✅ OpenCL 3.0+ ПОДДЕРЖИВАЕТСЯ!

📦 EXTENSIONS
─────────────────────────────────────────────────────
✅ cl_khr_command_buffer         ← ЕСТЬ!
✅ cl_khr_svm
✅ cl_amd_device_attribute_query

💾 SHARED VIRTUAL MEMORY (SVM) SUPPORT
─────────────────────────────────────────────────────
✅ CL_DEVICE_SVM_FINE_GRAIN_BUFFER (good!)
✅ CL_DEVICE_SVM_FINE_GRAIN_SYSTEM (excellent!)
✅ CL_DEVICE_SVM_ATOMICS

🎯 COMMAND BUFFERS (OpenCL 3.0) SUPPORT
─────────────────────────────────────────────────────
✅ cl_khr_command_buffer EXTENSION FOUND!
✅ COMMAND BUFFERS ARE SUPPORTED!
✅ Device is command buffer capable!

🎯 RECOMMENDATIONS FOR YOUR ALGORITHM
═══════════════════════════════════════════════════════
✅ OPTION 1 (BEST): Use Command Buffers!
   - Build 2 command buffers: STEP1, STEP2+3
   - Expected speedup: 15-25% (МАКСИМУМ!)
   - Code example in next section
```

---

## 📊 ИНТЕРПРЕТАЦИЯ РЕЗУЛЬТАТОВ

### Что означают проверки:

| Проверка | Означает | Для твоего алгоритма |
|----------|----------|---------------------|
| **OpenCL 3.0+** | Modern features | Нужно для Command Buffers |
| **cl_khr_command_buffer** | Command Buffers поддерживаются | ИДЕАЛЬНО! Делай 2-buffer |
| **cl_khr_svm** | Shared Virtual Memory | Хорошо для async операций |
| **Fine-grain SVM** | Лучший вариант SVM | Меньше синхронизаций |
| **Unified Memory** | GPU видит host память напрямую | Условие для SVM |

---

## 🎯 ДЕЙСТВИЯ ПО РЕЗУЛЬТАТАМ

### ЕСЛИ ЕСТЬ ✅ Command Buffers (cl_khr_command_buffer):

```cpp
// ДЕЛАЙ ЭТО:
// 1. Build Command Buffer 1: STEP 1
// 2. Build Command Buffer 2: STEP 2+3
// 3. Enqueue обе вместе
// Ожидаемое ускорение: 10-25%
```

**Файл с примером:** `COMMAND_BUFFERS_STRATEGY.md`

---

### ЕСЛИ ЕСТЬ ✅ Fine-Grain SVM, но НЕТ Command Buffers:

```cpp
// ДЕЛАЙ ЭТО:
// 1. Выделить SVM буферы: clSVMAlloc()
// 2. Host заполняет SVM напрямую: memcpy()
// 3. Добавлять FFT команды без WriteBuffer
// 4. GPU читает SVM данные автоматически
// Ожидаемое ускорение: 5-15%
```

**Файл с примером:** `SVM_GPU_QUEUE_LATENCY.md`

---

### ЕСЛИ ТОЛЬКО OpenCL 1.2:

```cpp
// ДЕЛАЙ ЭТО:
// 1. Используй асинхронные enqueue БЕЗ clWaitForEvents()
// 2. Добавлять команды в очередь все сразу
// 3. Вызвать clFinish() только в конце
// Ожидаемое ускорение: 2-5%
```

**Код:**
```cpp
// БЫЛО:
clEnqueueWriteBuffer(queue, input_data, ...);
clWaitForEvents(1, &event);  // ❌ БЛОКИРУЕТ
clfftEnqueueTransform(...);

// СТАЛО:
clEnqueueWriteBuffer(queue, input_data, ..., nullptr);  // ❌ БЕЗ wait
clfftEnqueueTransform(...);                              // сразу
clFinish(queue);  // ждём только в конце
```

---

## 🔍 ДОПОЛНИТЕЛЬНЫЕ ПРОВЕРКИ

### Если результаты странные, попробуй:

```bash
# 1. Проверить версию OpenCL драйвера
clinfo  # (если установлен)

# 2. Проверить видеокарту
nvidia-smi  # для NVIDIA
rocm-smi    # для AMD

# 3. Проверить переменные окружения
echo $LD_LIBRARY_PATH  # Linux
echo $PATH             # Windows
```

---

## 📞 ЕСЛИ ЕСТЬ ВОПРОСЫ

**Запусти этот скрипт и скинь мне вывод:**

```bash
./capability_checker > results.txt 2>&1
# Отправь мне results.txt
```

Я помогу интерпретировать результаты и выбрать лучшую стратегию! 🚀

---

## 💡 ЧТО ДАЛЬШЕ

1. **Запусти тестер** на своих картах (RTX 2080 Ti и RTX 3060)
2. **Посмотри результаты** - есть ли Command Buffers?
3. **Выбери стратегию** по таблице выше
4. **Реализуй оптимизацию** для твоего алгоритма
5. **Измерь выигрыш** в производительности

**Ожидаемый результат:** 10-25% ускорение всего алгоритма! 🎯
