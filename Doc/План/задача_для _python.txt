Цель: Написать инструкцию для Python, для создания такого же алгоритма, 
       что бы провести верификацию и валидацию алгоритма.
Напоминаю алгоритм
У нас 3 шага значения каждого шага записывается в файл json
0. Формируем sig0 = M-последовательность -. json

1. Шаг формирование набота спектров опорного сигнала для корреляции. Передаем sig0 в шаг 1 в нем 
    конвертируем данные в формат OpenCL complrx float затем методом циклической свертки в право 
    формируем набот массивов назовем их базовые сигналы для корреляции, потом нативной библиотекой 
    берем FFT сразу от всего вектора, после этого вызываем еще один callback для формирования сопряженного 
    комплексного сигнала  записываем в память и записываем в -> json

2. Шаг формируем спектр тестового сигнала. Берем sig0 вормируем из него набор из n сигналов, 
    вызывом callback из нативной функции, входной сигнал преобразуем в формат OpenCL complrx float 
    и передаем его на вычисление спектра  записываем в память и записываем в -> json.

3. Шаг реализуем корреляцию вызываем нативную функцию IFFT c двумя callback, первый перемнажает вектора 
    опорного сигнала и тестового сигнала полученный вектор передаем в IFFT после выполнения этой операции 
    вызываем второй callback для который считает магнитуду (огибающую) и выводит заданное колличество 
    точек каждого коррелятора. Выходной формат M[номер сигнала][номмер коррелятора][вектор першых n_kg значений]

Пожалуйста, напиши описание всего алгоритма. Особенно удели внимание на кастомные функции напиши описание каждой
и привиди пример реализации на Python
    