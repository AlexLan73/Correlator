Делаем - Корреляция через fft & IFFT после получения спектра опорного сигнала Step1 нам нужно сделать из него 
          комплексно сопряженное.
Цель: Добавить еще один callback в первый шаг после вызова FFT. во всех векторах поменять
       знак в комплексной части значения и переписать значение.


Переделываем Post-callback функцию в Step 3
Цель Выести вектор значений после IFFT колличество сейчас передается параметром
     вывести все значения от [0..n_kg] - если там комплекс то выводим магнитуду типа float
     если значение после IFFT магнитуда то выводим ее.
     посмотри в параметрах IFFT может сразу выводим магнитуду?

формат должен быть таким
1. M[номер луча][номер коррелятора][значения 0..n_kg-1]
2. да 
