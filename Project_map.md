# 🗺️ Project Map - Correlator

## 📋 Обзор проекта

**Correlator** - Multi-GPU FFT Benchmark проект для оптимизации FFT & IFFT операций с кастомными функциями на различных платформах (OpenCL, CUDA, ROCm).

### Основные цели
- Оптимизация FFT & IFFT операций
- Поддержка множественных GPU платформ
- Детальное профилирование производительности
- Генерация отчетов о производительности

---

## 📁 Структура каталогов

### Корневой каталог

#### Основные файлы
- **`CMakeLists.txt`** - Конфигурация сборки CMake
  - Настройка C++20 стандарта
  - Поиск и линковка OpenCL и clFFT библиотек
  - Создание исполняемого файла `Correlator`
  - Настройка директорий для отчетов

- **`main.cpp`** - Главная точка входа программы
  - Инициализация конфигурации и pipeline
  - Генерация тестовых данных (M-последовательности)
  - Выполнение Step 1, 2, 3 корреляции
  - Профилирование и генерация отчетов

- **`CLAUDE.md`** - Конфигурация AI ассистента
  - Настройки коммуникации
  - Правила работы с проектом
  - Информация о пользователе и проекте

- **`debug.sh`** - Скрипт для сборки Debug версии проекта
  - Очистка предыдущей отладочной сборки
  - Конфигурация CMake для Debug режима
  - Сборка с отладочными символами
  - Инструкции по использованию gdb и VS Code

- **`view_reports.sh`** - Скрипт для просмотра отчетов профилирования
  - Отображение последнего Markdown отчета
  - Просмотр последних JSON отчетов валидации
  - Навигация по каталогам отчетов

#### Каталоги сборки
- **`build/`** - Каталог сборки Release версии
- **`build_debug/`** - Каталог сборки Debug версии

> **Примечание:** Каталоги `build/` и `build_debug/` содержат сгенерированные файлы сборки и не описываются детально.

---

### 📂 `include/` - Заголовочные файлы

#### `include/correlator/` - Основная архитектура проекта

**Интерфейсы (I-prefix):**
- **`IConfiguration.hpp`** - Интерфейс конфигурации коррелятора
  - Хранение параметров (FFT size, num shifts, num signals, etc.)
  - Валидация параметров
  - JSON сериализация/десериализация

- **`IDataSnapshot.hpp`** - Интерфейс сохранения промежуточных данных
  - Сохранение данных на каждом этапе (Step 1, 2, 3)
  - Экспорт в JSON формат
  - Статистика данных

- **`IFFTBackend.hpp`** - Интерфейс FFT бэкенда (Strategy Pattern)
  - Абстракция платформы (OpenCL, CUDA, ROCm)
  - Методы для выполнения шагов корреляции
  - Получение результатов вычислений
  - Получение информации об устройстве

- **`IDataValidator.hpp`** - Интерфейс валидации данных
  - Валидация на каждом этапе
  - Проверка размеров, диапазонов значений
  - Сравнение с эталонными данными

- **`IResultExporter.hpp`** - Интерфейс экспорта результатов
  - Экспорт в JSON на каждом этапе
  - Финальный отчет

**Реализации:**
- **`Configuration.hpp`** - Реализация конфигурации с валидацией
- **`DataSnapshot.hpp`** - Реализация сохранения промежуточных данных
- **`DataValidator.hpp`** - Реализация валидации с проверками
- **`ResultExporter.hpp`** - Реализация экспорта в JSON файлы
- **`OpenCLFFTBackend.hpp`** - Реализация OpenCL бэкенда (адаптер над FFTHandler)
- **`CorrelationPipeline.hpp`** - Главный класс оркестрации всего pipeline
  - Управление выполнением Step 1, 2, 3
  - Сохранение данных профилирования (OperationTiming)
  - Интеграция валидации и экспорта

**Документация:**
- **`README.md`** - Общая документация по архитектуре
- **`ARCHITECTURE.md`** - Детальное описание архитектуры проекта
  - SOLID принципы
  - Паттерны проектирования
  - Workflow выполнения
  - Примеры использования

**Общий заголовок:**
- **`Correlator.hpp`** - Главный заголовочный файл, включающий все необходимые компоненты

#### `include/` - Дополнительные заголовочные файлы

- **`fft_handler.hpp`** - Класс FFTHandler для низкоуровневой работы с OpenCL FFT
  - Управление OpenCL контекстом, очередью, устройствами
  - Создание и управление clFFT планами
  - Выполнение Step 1, 2, 3 операций
  - Профилирование операций (OperationTiming)
  - Управление буферами GPU

- **`profiler.hpp`** - Класс Profiler для профилирования производительности
  - Измерение времени выполнения операций
  - Профилирование OpenCL событий
  - Экспорт отчетов в Markdown и JSON форматы
  - Получение информации о GPU

- **`cpu_converter.hpp`** - Конвертация данных на CPU
- **`gpu_converter.hpp`** - Конвертация данных на GPU

---

### 📂 `src/` - Исходные файлы реализации

- **`fft_handler.cpp`** - Реализация FFTHandler
  - Инициализация OpenCL контекста и устройств
  - Создание clFFT планов с pre/post callbacks
  - Реализация Step 1: Reference Signals + Forward FFT
  - Реализация Step 2: Input Signals + Forward FFT
  - Реализация Step 3: Correlation + IFFT
  - Управление буферами и событиями OpenCL
  - Детальное профилирование операций

- **`cpu_converter.cpp`** - Реализация CPU конвертации
- **`gpu_converter.cpp`** - Реализация GPU конвертации

#### OpenCL Kernel файлы (`.cl`)

- **`fft_correlator_kernel.cl`** - OpenCL kernel для корреляции FFT
- **`gpu_converter_kernel.cl`** - OpenCL kernel для конвертации данных на GPU
- **`pre_post_callbacks.cl`** - OpenCL callbacks для clFFT планов
  - Pre-callback: конвертация int32 → float2 с масштабированием
  - Post-callback: вычисление корреляции и поиск пиков
- **`step3_correlation_kernel.cl`** - OpenCL kernel для Step 3 корреляции

#### Дополнительные файлы

- **`main_step3.cpp`** - Альтернативная реализация main для Step 3
- **`step3_implementation.cpp`** - Реализация Step 3 корреляции

---

### 📂 `Doc/` - Документация и планы

- **`CLEANUP_FIX_GUIDE.md`** - Руководство по исправлению cleanup
- **`CLEANUP_FIX_SUMMARY.md`** - Резюме исправлений cleanup
- **`cleanup_SAFE.cpp`** - Безопасная версия cleanup кода
- **`fft_handler_SAFE.hpp`** - Безопасная версия заголовка fft_handler

#### `Doc/План/` - Планы разработки

- **`ФормированиеОтчета.txt`** - План формирования отчетов профилирования
- **`EditCallBack.txt`** - План редактирования callbacks

---

### 📂 `examples/` - Примеры использования

- **`basic_usage.cpp`** - Базовый пример использования Correlator API

---

### 📂 `MemoryBank/` - Память сессий и ключевые находки

- **`AI_SESSION_MEMORY.md`** - Память текущей сессии работы с AI
- **`key_findings.md`** - Ключевые результаты исследований
  - FFT оптимизации
  - GPU occupancy анализ
  - Correlation архитектура
  - Sliding FFT анализ
  - GitHub MCP интеграция

---

### 📂 `Report/` - Отчеты профилирования и валидации

Каталог содержит сгенерированные отчеты о производительности и результаты валидации:

- **`profiling_report_*.md`** - Markdown отчеты профилирования
  - Общая статистика выполнения
  - Детальное профилирование по шагам (Step 1, 2, 3)
  - Информация о GPU и системе
  - Временные метрики операций

- **`JSON/`** - JSON отчеты профилирования
  - Структурированные данные о производительности
  - Детальные метрики операций
  - Данные для автоматического анализа

- **`Validation/`** - JSON отчеты валидации данных
  - Результаты валидации на каждом этапе (Step 1, 2, 3)
  - Файлы формата `validation_step[N]_[timestamp].json`
  - Проверка корректности промежуточных данных

> **Примечание:** Отчеты генерируются автоматически при каждом запуске программы. Каталог может содержать как файлы в корне `Report/`, так и в `build/Report/` и `build_debug/Report/`.

---

## 🔄 Workflow выполнения

```
1. Инициализация
   ├─ Создание конфигурации (IConfiguration)
   ├─ Создание бэкенда (IFFTBackend::createOpenCLBackend)
   └─ Создание pipeline (CorrelationPipeline)

2. Step 1: Reference Signals + Forward FFT
   ├─ Загрузка reference сигнала на GPU
   ├─ Pre-callback: конвертация int32 → float2
   ├─ Forward FFT (batch обработка)
   ├─ Сохранение в snapshot
   ├─ Валидация
   └─ Экспорт в JSON

3. Step 2: Input Signals + Forward FFT
   ├─ Загрузка input сигналов на GPU
   ├─ Pre-callback: конвертация int32 → float2
   ├─ Forward FFT (batch обработка)
   ├─ Сохранение в snapshot
   ├─ Валидация
   └─ Экспорт в JSON

4. Step 3: Correlation + IFFT
   ├─ Умножение FFT результатов
   ├─ Post-callback: вычисление корреляции и поиск пиков
   ├─ Inverse FFT
   ├─ Загрузка результатов с GPU
   ├─ Сохранение в snapshot
   ├─ Валидация
   └─ Экспорт в JSON

5. Генерация отчетов
   ├─ Markdown отчет (profiling_report_*.md)
   └─ JSON отчет (profiling_report_*.json)
```

---

## 🛠️ Технологии и зависимости

### Основные библиотеки
- **OpenCL** - API для параллельных вычислений на GPU/CPU
- **clFFT** - OpenCL библиотека для FFT операций
- **C++20** - Стандарт языка программирования

### Системные библиотеки
- `pthread` - Потоки
- `rt` - Real-time расширения
- `m` - Математические функции

---

## 📊 Профилирование

Проект включает детальное профилирование всех операций:
- **Upload/Download операции** - время загрузки/выгрузки данных на/с GPU
- **FFT операции** - время выполнения Forward/Inverse FFT
- **Callback операции** - время выполнения pre/post callbacks
- **Общее время GPU** - суммарное время выполнения на GPU
- **CPU wait time** - время ожидания CPU

Все данные профилирования сохраняются в отчеты (Markdown и JSON).

---

## 🎯 Ключевые особенности архитектуры

### SOLID принципы
- **Single Responsibility** - каждый класс имеет одну ответственность
- **Open/Closed** - открыт для расширения, закрыт для модификации
- **Liskov Substitution** - интерфейсы могут быть заменены реализациями
- **Interface Segregation** - маленькие, специфичные интерфейсы
- **Dependency Inversion** - зависимость от абстракций

### Паттерны проектирования
- **Strategy Pattern** - `IFFTBackend` для переключения между платформами
- **Factory Pattern** - фабричные методы для создания объектов
- **Builder Pattern** - `CorrelationPipeline` для построения сложных объектов
- **Observer Pattern** - валидация и экспорт на каждом этапе

---

## 📝 Примечания

- Каталоги `build/` и `build_debug/` исключены из детального описания (содержат сгенерированные файлы сборки)
- Отчеты могут создаваться как в корневом `Report/`, так и в `build/Report/` и `build_debug/Report/`
- Все пути указаны относительно корневого каталога проекта

---

*Последнее обновление: 2025-12-23*  
*Версия: 1.1*

