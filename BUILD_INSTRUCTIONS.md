# 📋 Инструкция по сборке и запуску проекта

## 🔧 Режимы сборки

Проект поддерживает 3 режима сборки:
- **Release** - оптимизированная сборка без debug вывода (для production)
- **Debug** - сборка с отладочной информацией и полным выводом
- **RelWithDebInfo** - оптимизированная сборка с отладочными символами

---

## 🚀 Способ 1: Через терминал (CMake напрямую)

### Release режим (рекомендуется для production):

```bash
# 1. Очистить предыдущую сборку (если нужно)
rm -rf build

# 2. Создать Release конфигурацию
cmake -B build -DCMAKE_BUILD_TYPE=Release

# 3. Собрать проект
cmake --build build

# 4. Запустить
./build/Correlator
```

**Что отключено в Release:**
- ✅ `DEBUG_LOG` - отладочные сообщения
- ✅ `VERBOSE_LOG` - детальные отладочные сообщения
- ✅ `[PROFILE]` - профилирование операций
- ✅ `[DEBUG]` - все debug сообщения

**Что включено в Release:**
- ✅ `INFO_LOG` - важные сообщения (`[STEP 1]`, `[OK]`, `[FFT]`)
- ✅ `std::cout` из `main.cpp` - пользовательский интерфейс
- ✅ `ERROR_LOG` / `WARNING_LOG` - ошибки и предупреждения

---

### Debug режим (для отладки):

```bash
# 1. Очистить предыдущую сборку (если нужно)
rm -rf build_debug

# 2. Создать Debug конфигурацию
cmake -B build_debug -DCMAKE_BUILD_TYPE=Debug

# 3. Собрать проект
cmake --build build_debug

# 4. Запустить
./build_debug/Correlator
```

**Что включено в Debug:**
- ✅ ВСЕ сообщения (`DEBUG_LOG`, `VERBOSE_LOG`, `INFO_LOG`)
- ✅ Полный debug вывод
- ✅ Профилирование операций
- ✅ Детальная диагностика

---

## 🎯 Способ 2: Через VS Code

### Запуск в Release режиме:

1. **Откройте VS Code** в корне проекта

2. **Нажмите `F5`** или:
   - Перейдите в меню: `Run > Start Debugging`
   - Выберите конфигурацию: **"Launch Correlator (Release)"**

3. VS Code автоматически:
   - Выполнит задачу `Build (Release)`
   - Соберет проект в Release режиме
   - Запустит программу

### Запуск в Debug режиме:

1. **Нажмите `F5`**
2. Выберите конфигурацию: **"Launch Correlator (Debug)"**

---

## 🔍 Проверка режима сборки

### Проверить, какой режим активен:

```bash
# Проверить флаги компиляции
cat build/CMakeFiles/Correlator.dir/flags.make | grep NDEBUG

# Если видите "-DNDEBUG" - это Release режим ✅
# Если НЕ видите "-DNDEBUG" - это Debug режим
```

### Проверить, что DEBUG_LOG отключен в Release:

```bash
# Запустить программу и проверить вывод
./build/Correlator 2>&1 | grep -E "\[PROFILE\]|\[DEBUG\]|PROFILING RESULTS"

# Если ничего не выводится - значит DEBUG_LOG отключен ✅
```

---

## 📝 Сборка через задачи VS Code

В VS Code доступны задачи (Ctrl+Shift+P → "Tasks: Run Task"):

- **"Build (Release)"** - собрать Release версию
- **"Build (Debug)"** - собрать Debug версию
- **"CMake Configure (Release)"** - только конфигурация Release
- **"CMake Configure (Debug)"** - только конфигурация Debug

---

## ⚠️ Важные замечания

1. **После изменения CMakeLists.txt** нужно переконфигурировать:
   ```bash
   cmake -B build -DCMAKE_BUILD_TYPE=Release
   ```

2. **После изменения кода** достаточно пересобрать:
   ```bash
   cmake --build build
   ```

3. **Для полной пересборки:**
   ```bash
   rm -rf build
   cmake -B build -DCMAKE_BUILD_TYPE=Release
   cmake --build build
   ```

4. **Проверка что NDEBUG определен:**
   - В Release режиме компилятор должен получить флаг `-DNDEBUG`
   - Это отключает все `DEBUG_LOG` и `VERBOSE_LOG`

---

## 🐛 Устранение проблем

### Проблема: DEBUG_LOG всё равно выводится в Release

**Решение:**
```bash
# 1. Полностью очистить build
rm -rf build

# 2. Пересоздать конфигурацию
cmake -B build -DCMAKE_BUILD_TYPE=Release

# 3. Проверить что NDEBUG определен
grep NDEBUG build/CMakeFiles/Correlator.dir/flags.make

# 4. Пересобрать
cmake --build build --clean-first

# 5. Проверить вывод
./build/Correlator 2>&1 | grep -i "\[profile\]\|\[debug\]" || echo "✅ DEBUG_LOG отключен!"
```

### Проблема: VS Code запускает не тот режим

**Решение:**
1. Убедитесь что выбрана правильная конфигурация запуска:
   - `F5` → выбрать "Launch Correlator (Release)"
2. Проверьте `.vscode/launch.json` - там должны быть настройки для Release
3. Убедитесь что задача "Build (Release)" выполняется перед запуском

---

## ✅ Быстрая проверка

```bash
# Собрать Release
rm -rf build && cmake -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build

# Запустить и проверить что DEBUG вывод отключен
./build/Correlator 2>&1 | grep -E "\[PROFILE\]|\[DEBUG\]|PROFILING RESULTS" && echo "❌ DEBUG включен!" || echo "✅ DEBUG отключен!"
```

---

**Дата создания:** 2025-12-24  
**Версия:** 1.0

